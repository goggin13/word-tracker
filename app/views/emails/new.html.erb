<h1>Word of the Day</h1>

<p>
  <%= link_to @word.text, word_url(@word) %>
</p>

<% @tags.each do |tag| %>

  <h1><%= tag.name %></h1>

  <% tag.random_notes(1).each do |note| %>
    <p>
      <%= note.front_rendered %>
      <%= link_to "View", note_url(note) %>
    </p>
  <% end %>
<% end %>
